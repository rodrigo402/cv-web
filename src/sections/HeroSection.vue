<script setup lang="ts">
import { profile } from '@/data/cv'

const cvPdfUrl = `${import.meta.env.BASE_URL}rodrigo-gabriel-reynoso-cv.pdf`

function scrollTo(id: string) {
  const el = document.getElementById(id)
  if (el) {
    const top = el.getBoundingClientRect().top + window.scrollY - 72
    window.scrollTo({ top, behavior: 'smooth' })
  }
}
</script>

<template>
  <section id="hero" class="hero-section" aria-label="Presentación">
    <div class="orb" aria-hidden="true" />

    <div class="hero-inner">
      <div class="hero-text">
        <h1 class="hero-name gradient-text">{{ profile.name }}</h1>

        <p class="hero-title">
          <span class="mono-chip">Flutter Developer</span>
          <span class="hero-tagline">{{ profile.tagline }}</span>
        </p>

        <p class="hero-bio">{{ profile.bio }}</p>

        <div class="hero-cta">
          <a href="#experience" class="btn btn-primary" @click.prevent="scrollTo('experience')">
            Ver experiencia
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2.5" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <line x1="5" y1="12" x2="19" y2="12"/><polyline points="12 5 19 12 12 19"/>
            </svg>
          </a>
          <a :href="cvPdfUrl" download="Rodrigo-Reynoso-CV.pdf" class="btn btn-outline">
            <svg width="14" height="14" viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" aria-hidden="true">
              <path d="M21 15v4a2 2 0 0 1-2 2H5a2 2 0 0 1-2-2v-4"/><polyline points="7 10 12 15 17 10"/><line x1="12" y1="15" x2="12" y2="3"/>
            </svg>
            Descargar CV
          </a>
        </div>

        <div class="hero-socials">
          <a :href="profile.github" target="_blank" rel="noopener noreferrer" aria-label="GitHub" class="icon-link">
            <svg viewBox="0 0 24 24" fill="currentColor" width="18" height="18" aria-hidden="true">
              <path d="M12 2C6.477 2 2 6.484 2 12.017c0 4.425 2.865 8.18 6.839 9.504.5.092.682-.217.682-.483 0-.237-.008-.868-.013-1.703-2.782.605-3.369-1.343-3.369-1.343-.454-1.158-1.11-1.466-1.11-1.466-.908-.62.069-.608.069-.608 1.003.07 1.531 1.032 1.531 1.032.892 1.53 2.341 1.088 2.91.832.092-.647.35-1.088.636-1.338-2.22-.253-4.555-1.113-4.555-4.951 0-1.093.39-1.988 1.029-2.688-.103-.253-.446-1.272.098-2.65 0 0 .84-.27 2.75 1.026A9.564 9.564 0 0 1 12 6.844a9.59 9.59 0 0 1 2.504.337c1.909-1.296 2.747-1.027 2.747-1.027.546 1.379.202 2.398.1 2.651.64.7 1.028 1.595 1.028 2.688 0 3.848-2.339 4.695-4.566 4.943.359.309.678.92.678 1.855 0 1.338-.012 2.419-.012 2.747 0 .268.18.58.688.482A10.02 10.02 0 0 0 22 12.017C22 6.484 17.522 2 12 2z"/>
            </svg>
          </a>
          <a :href="profile.linkedin" target="_blank" rel="noopener noreferrer" aria-label="LinkedIn" class="icon-link">
            <svg viewBox="0 0 24 24" fill="currentColor" width="18" height="18" aria-hidden="true">
              <path d="M20.447 20.452h-3.554v-5.569c0-1.328-.027-3.037-1.852-3.037-1.853 0-2.136 1.445-2.136 2.939v5.667H9.351V9h3.414v1.561h.046c.477-.9 1.637-1.85 3.37-1.85 3.601 0 4.267 2.37 4.267 5.455v6.286zM5.337 7.433a2.062 2.062 0 0 1-2.063-2.065 2.064 2.064 0 1 1 2.063 2.065zm1.782 13.019H3.555V9h3.564v11.452zM22.225 0H1.771C.792 0 0 .774 0 1.729v20.542C0 23.227.792 24 1.771 24h20.451C23.2 24 24 23.227 24 22.271V1.729C24 .774 23.2 0 22.222 0h.003z"/>
            </svg>
          </a>
          <a :href="`mailto:${profile.email}`" aria-label="Enviar email" class="icon-link">
            <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="1.75" stroke-linecap="round" stroke-linejoin="round" width="18" height="18" aria-hidden="true">
              <path d="M4 4h16c1.1 0 2 .9 2 2v12c0 1.1-.9 2-2 2H4c-1.1 0-2-.9-2-2V6c0-1.1.9-2 2-2z"/><polyline points="22,6 12,13 2,6"/>
            </svg>
          </a>
        </div>

        <p class="hero-location" aria-label="Ubicación">
          <svg viewBox="0 0 24 24" fill="none" stroke="currentColor" stroke-width="2" stroke-linecap="round" stroke-linejoin="round" width="13" height="13" aria-hidden="true">
            <path d="M21 10c0 7-9 13-9 13s-9-6-9-13a9 9 0 0 1 18 0z"/><circle cx="12" cy="10" r="3"/>
          </svg>
          {{ profile.location }} · Disponible para trabajo remoto
        </p>
      </div>

      <div class="hero-visual" aria-hidden="true">
        <div class="photo-frame">
          <img :src="profile.photo" alt="Rodrigo Reynoso" class="photo" />
        </div>
        <div class="photo-chip">
          <span class="badge-dot" />
          <span>Flutter Developer</span>
        </div>
      </div>
    </div>

    <div class="scroll-cue" aria-hidden="true">
      <span>Scroll</span>
      <span class="scroll-line" />
    </div>
  </section>
</template>

<style scoped>
.hero-section {
  position: relative;
  min-height: 100vh;
  display: flex;
  flex-direction: column;
  justify-content: center;
  overflow: hidden;
}

.orb {
  position: fixed;
  top: -25vh;
  right: -15vw;
  width: 65vw;
  height: 65vw;
  max-width: 680px;
  max-height: 680px;
  border-radius: 50%;
  background: radial-gradient(circle at center, rgba(139, 92, 246, 0.08) 0%, transparent 65%);
  pointer-events: none;
  z-index: 0;
}
[data-theme='light'] .orb {
  background: radial-gradient(circle at center, rgba(124, 58, 237, 0.05) 0%, transparent 65%);
}

.hero-inner {
  position: relative;
  z-index: 1;
  max-width: var(--max-w);
  margin: 0 auto;
  padding: calc(var(--nav-h) + 4.5rem) var(--pad) 4rem;
  display: grid;
  grid-template-columns: 1fr auto;
  gap: 5rem;
  align-items: center;
  width: 100%;
}

.hero-text {
  display: flex;
  flex-direction: column;
  gap: 1.375rem;
  max-width: 580px;
}

.hero-name {
  line-height: 1.02;
  margin-top: 0.25rem;
}

.hero-title {
  display: flex;
  align-items: center;
  flex-wrap: wrap;
  gap: 0.625rem;
}

.hero-tagline {
  font-size: 0.9375rem;
  color: var(--text-3);
}

.hero-bio {
  font-size: 1.0625rem;
  line-height: 1.75;
  max-width: 500px;
}

.hero-cta {
  display: flex;
  gap: 0.75rem;
  flex-wrap: wrap;
}

.hero-socials {
  display: flex;
  gap: 0.5rem;
}

.hero-location {
  display: inline-flex;
  align-items: center;
  gap: 0.375rem;
  font-size: 0.8125rem;
  color: var(--text-3);
  margin-top: 0.25rem;
}

.hero-visual {
  position: relative;
  flex-shrink: 0;
}

.photo-frame {
  width: 260px;
  height: 260px;
  border-radius: 50%;
  padding: 3px;
  background: linear-gradient(135deg, var(--accent), rgba(139, 92, 246, 0.1) 60%, var(--accent-3));
  box-shadow:
    0 8px 24px rgba(0, 0, 0, 0.18),
    var(--accent-glow);
  animation: float 6s ease-in-out infinite;
}

.photo {
  width: 100%;
  height: 100%;
  border-radius: 50%;
  object-fit: cover;
  object-position: center 18%;
  border: 3px solid #fff;
  display: block;
}

[data-theme='dark'] .photo {
  border-color: var(--bg);
}

.photo-chip {
  position: absolute;
  bottom: 8px;
  right: -12px;
  display: flex;
  align-items: center;
  gap: 0.4rem;
  background: var(--surface);
  border: 1px solid var(--border);
  border-radius: 100px;
  padding: 0.35rem 0.8rem;
  font-size: 0.75rem;
  font-weight: 500;
  color: var(--text-2);
  box-shadow: 0 4px 20px rgba(0, 0, 0, 0.25);
  white-space: nowrap;
}

.scroll-cue {
  position: relative;
  z-index: 1;
  display: flex;
  align-items: center;
  gap: 0.75rem;
  max-width: var(--max-w);
  margin: 0 auto;
  padding: 0 var(--pad) 2rem;
  font-family: var(--font-mono);
  font-size: 0.68rem;
  letter-spacing: 0.1em;
  color: var(--text-3);
  text-transform: uppercase;
}
.scroll-line {
  display: block;
  width: 36px;
  height: 1px;
  background: var(--border-2);
}

@media (max-width: 780px) {
  .hero-inner {
    grid-template-columns: 1fr;
    text-align: center;
    gap: 2.5rem;
    padding-top: calc(var(--nav-h) + 3rem);
  }
  .hero-visual {
    order: -1;
    display: flex;
    justify-content: center;
  }
  .photo-frame {
    width: 190px;
    height: 190px;
    animation: none;
  }
  .photo-chip { bottom: 4px; right: 0; }
  .hero-text { align-items: center; }
  .hero-bio { text-align: center; }
  .hero-cta { justify-content: center; }
  .hero-socials { justify-content: center; }
  .hero-title { justify-content: center; }
  .hero-location { justify-content: center; }
  .scroll-cue { display: none; }
}

@media (max-width: 480px) {
  .hero-cta { flex-direction: column; align-items: stretch; }
  .btn { justify-content: center; }
}

.print-hint {
  position: fixed;
  bottom: 1.5rem;
  left: 50%;
  transform: translateX(-50%);
  z-index: 200;
  display: flex;
  align-items: center;
  gap: 0.6rem;
  background: var(--surface);
  border: 1px solid var(--accent);
  border-radius: var(--radius-lg);
  padding: 0.75rem 1.25rem;
  font-size: 0.8125rem;
  color: var(--text-2);
  box-shadow: 0 8px 32px rgba(0,0,0,0.3), var(--accent-glow);
  white-space: nowrap;
  pointer-events: none;
}

.print-hint strong {
  color: var(--accent);
  font-weight: 600;
}

.print-hint svg {
  color: var(--accent);
  flex-shrink: 0;
}

.hint-enter-active, .hint-leave-active {
  transition: opacity 0.25s ease, transform 0.25s ease;
}
.hint-enter-from, .hint-leave-to {
  opacity: 0;
  transform: translateX(-50%) translateY(8px);
}

@media (max-width: 600px) {
  .print-hint {
    white-space: normal;
    max-width: calc(100vw - 2rem);
    text-align: center;
    flex-wrap: wrap;
    justify-content: center;
  }
}

@media (prefers-reduced-motion: reduce) {
  .photo-frame { animation: none; }
}

@media print {
  .print-hint { display: none !important; }
}
</style>
